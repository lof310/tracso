# Development and CI dependencies for tracso.
#
# Install:
#     pip install -r requirements-dev.txt
#
# Runtime optional features are also pulled in so the full test
# matrix can run.

# --- runtime (optional features, for testing) --------------------------
textual>=0.40
loguru>=0.7

# --- packaging --------------------------------------------------------
build>=1.0
twine>=4.0
hatchling>=1.20

# --- testing ----------------------------------------------------------
pytest>=7.0
pytest-cov>=4.0

# --- linting and formatting -------------------------------------------
ruff>=0.4
mypy>=1.8
types-psutil>=5.9    # only if psutil gets added later
black
isort

# --- release hygiene --------------------------------------------------
pre-commit>=3.5
